@model IEnumerable<QUYNH.Models.KhachHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">

    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HoTen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NamSinh)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.Raw( item.HoTen)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.NamSinh)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.HoTen }) |
        @Html.ActionLink("Details", "Details", new { id = item.HoTen }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.HoTen })
    </td>
</tr>
}

</table>
